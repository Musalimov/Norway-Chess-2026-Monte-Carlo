# bin/

This folder is intentionally reserved for local build artifacts.

The project does not commit compiled binaries. They are generated when you run:

```bash
python3 tools/run_tests.py
```

Expected generated file:

```text
bin/test_model
```
